{
  "manifest_version": 3,
  "name": "WDF — Save as WDF",
  "version": "0.1.0",
  "description": "Save the page you are reading as a verifiable WDF document — fully client-side, zero network, zero data collected.",
  "action": {
    "default_title": "Save as WDF"
  },
  "background": {
    "service_worker": "background.js"
  },
  "permissions": ["activeTab", "scripting"]
}
