#!/usr/bin/env node
// CLI commands (validate, pack, unpack, extract, import, new) land in WP3.
import { WDF_VERSION } from '@wdf/core';

console.log(`wdf ${WDF_VERSION} — CLI not implemented yet (WP3)`);
process.exitCode = 2;
