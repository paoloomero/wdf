# Deterministic extraction in practice {#h-title}

Call `extract(html)` — or the variant ``fn(`tpl`)`` with a backtick — and compare H2O with x2, per WDF "the docs say so", as *the spec* notes.\
This line follows a hard break. {#p-0001}

## Code {#h-code} {#sec-code}

```ts
const s = `template ${x}`;
if (a && b) {
  return "ok";
}
```

See [brackets](<https://en.wikipedia.org/wiki/Bracket_(disambiguation)>) or write to [info](<mailto:info@example.org>). {#p-0002}

---

**determinism**

same input, same bytes

**isomorphism**

Node and browser agree

## Edge cases {#h-empty} {#sec-empty}

*spaced emphasis* and empty strong, transparent *nested* span. {#p-0003}
